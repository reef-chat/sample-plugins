{
  "id": "com.example.reef-dyntool-html-fetcher",
  "manifest_version": "1",
  "kind": "dynamic_tool",
  "version": "0.3.0",
  "name": "HTML fetcher",
  "description": "Two dynamic tools: fetch a URL and return text, or extract <title>/<meta description>.",
  "license": "MIT",
  "tools": [
    {
      "name": "fetch_url_text",
      "description": "GET a URL and return the response body as plain text (truncated to 200 KB).",
      "parameters": {
        "type": "object",
        "properties": {
          "url": { "type": "string", "description": "Absolute https URL" }
        },
        "required": ["url"]
      },
      "body": "const res = await fetch(args.url, { redirect: 'follow' });\nif (!res.ok) return { ok: false, status: res.status };\nconst text = (await res.text()).slice(0, 200 * 1024);\nreturn { ok: true, status: res.status, text };"
    },
    {
      "name": "extract_page_summary",
      "description": "GET a URL and return its <title> and <meta name=\"description\"> values.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": { "type": "string", "description": "Absolute https URL" }
        },
        "required": ["url"]
      },
      "entry": "tools/extract-page-summary.js"
    }
  ],
  "declared_capabilities": {
    "network_domains": ["*"]
  }
}
